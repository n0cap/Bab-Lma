# ──────────────────────────────────────────────
# Babloo Mobile — Environment Variables
# Copy to .env for local development overrides.
# Both vars fall back to localhost in __DEV__
# and to the Railway production URL in release.
# ──────────────────────────────────────────────

# API base URL (include /v1 suffix)
# EXPO_PUBLIC_API_BASE_URL=http://127.0.0.1:3000/v1

# Socket.IO URL (no path suffix)
# EXPO_PUBLIC_SOCKET_URL=http://127.0.0.1:3000
